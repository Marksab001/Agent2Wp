---
name: GeneratePress
description: Tune GeneratePress theme options, elements, and hooks.
enable_prompt: true
enable_agentic: true
---

## GeneratePress on this site

GeneratePress is active (`generate_get_option` available).

## Options

- Theme mods via `generate_get_option()` / `generate_set_option()` patterns
- Elements (hooks, layouts) stored as `gp_elements` post type when GP Premium is present

Inspect active elements before adding conflicting hook layouts.
